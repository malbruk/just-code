{
  "name": "@just-code/core",
  "version": "0.0.0",
  "private": true,
  "description": "IDE-agnostic shared layer for Just Code (protocol contract + agent core). Consumed by the VS Code extension, the shared webview UI, and the IntelliJ Node sidecar.",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*"
  }
}
